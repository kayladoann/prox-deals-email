# Prox Deals Email - Environment Variables
# Copy this file to .env and fill in your values

# ============================================
# DATABASE CONFIGURATION
# ============================================

# Database type: 'sqlite' (default) or 'supabase'
DB_TYPE=sqlite

# SQLite database path (used when DB_TYPE=sqlite)
SQLITE_PATH=./data/prox.db

# Supabase credentials (used when DB_TYPE=supabase)
# Get these from your Supabase project settings
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here

# ============================================
# EMAIL CONFIGURATION
# ============================================

# Email provider: 'resend' or 'console' (default)
# Use 'console' for local testing without sending real emails
EMAIL_PROVIDER=console

# Resend API key (used when EMAIL_PROVIDER=resend)
# Get your API key from https://resend.com/api-keys
RESEND_API_KEY=re_your_api_key_here

# From email address (must be verified domain in Resend)
FROM_EMAIL=deals@joinprox.com
